language: python
python:
 - "2.7"
services: 
 - elasticsearch
build_image: shippable/minv2
install: pip install --use-mirrors -r requirements.txt
script:
 - python sample.py
notifications:
  email:
    - varsha@shippable.com
archive: true
#cache: true
